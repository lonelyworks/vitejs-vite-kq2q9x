import { LucideIcon } from 'lucide-react';

export interface ServiceCardProps {
  Icon: LucideIcon;
  title: string;
  description: string;
}

export interface BuildFeatureProps {
  Icon: LucideIcon;
  title: string;
  description: string;
}

export interface BuildReasonProps {
  Icon: LucideIcon;
  title: string;
  description: string;
}